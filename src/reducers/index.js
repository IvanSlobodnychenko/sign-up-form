const initState = {
    // your state
};

const reducer = (state = initState, action) => {
    switch (action.type) {
        /*
        case 'SET_CURRENT_STEP':
            return {
                step: action.payload
            };
        */

            // ...

        default:
            return state;
    }
};

export default reducer;